<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %></li>
    <%= link_to "Delete", task_path(task), method: :delete, data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
  <% end %>
</ul>
 <button><%= link_to "new task", new_task_path %></button>
<%# ? for boolean %>
